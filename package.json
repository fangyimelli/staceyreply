{
  "name": "stacey-chart-reply",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "dev": "npm run build && python3 -m http.server 4173",
    "checklist": "node scripts/generateChecklist.js",
    "test": "npm run build"
  }
}
